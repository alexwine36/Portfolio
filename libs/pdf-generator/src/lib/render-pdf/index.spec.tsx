import RenderPDF from './index';

describe('RenderPDF', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<RenderPDF />);
    // expect(baseElement).toBeTruthy();

    RenderPDF({});
  });
});
