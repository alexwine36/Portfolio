import { softSkillArray } from './soft-skills';

describe('Soft Skills', () => {
  test('should return soft skills', async () => {
    // Object.keys(softSkills).forEach((v) => {
    //   const res = getSkill(v as any);
    //   console.log(res);
    // });
    console.log(softSkillArray);
  });
});
